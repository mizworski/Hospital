#include <stdio.h>

void readUntilEOF (void) {
    char *line = NULL;
    int lineLength;
    while (read = getline(&line, &lineLength, stdin)) {
        printf("chuj");
    }
}

void readSingleLine (char **) {
    ;
}